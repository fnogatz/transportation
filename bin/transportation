#!/usr/bin/env node

var program = require('commander');

program
  .version(require('../package.json').version)
  .command('positions', 'Export positions')
  .command('shapes', 'Export shapes')
  .parse(process.argv);
