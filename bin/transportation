#!/usr/bin/env node

const program = require('commander')

program
  .version(require('../package.json').version)
  .command('positions', "Create newline-separated GeoJSON Features of all the trips' positions")
  .command('shapes', 'Export shapes')
  .parse(process.argv)
